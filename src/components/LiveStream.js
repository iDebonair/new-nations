// LiveStream.js
import React from 'react';

const LiveStream = () => {
  return (
    <div>
      <h2>Live Stream</h2>
      {/* Embed live stream player or link to live stream page */}
      <p>Embed live stream here</p>
    </div>
  );
};

export default LiveStream;
